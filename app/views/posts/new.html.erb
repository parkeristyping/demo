<h1>New Post</h1>

<%= form_with(model: @post) do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <br/>

  <%= f.label :mood %>
  <%= f.select :mood, ['happy', 'neutral', 'sad'] %>
  <br/>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', posts_path %>
